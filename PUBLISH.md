# 电子量角器 · 真机测试与发布完整流程（新手版）

本指南面向**第一次开发微信小程序**的你，按时间顺序带你走完：注册账号 → 打开工程 → 模拟器验证 → 真机预览/调试 → 上传代码 → 提交审核 → 正式发布。所有步骤均在图形界面完成，不需要写命令。

> ⚠️ **第一步必须做**：当前 `project.config.json` 里 `"appid": "touristappid"` 是「游客模式」占位符，**只能本地预览，不能真机调试、不能上传、不能发布**。请先按第 2 步换成你自己的小程序 AppID。

---

## 第 1 步：注册微信小程序账号（约 10 分钟）

1. 打开 https://mp.weixin.qq.com → 右上角「立即注册」→ 选择「小程序」。
2. 用**未绑定过公众号**的邮箱注册，激活邮箱后登录。
3. 主体类型选择：
   - **个人**：免费、最快，但部分类目/能力受限，且无微信支付。**量角器这类「工具」类目个人可以做**。
   - **企业/个体工商户**：需营业执照，可做全功能、更易建立信任、审核更顺。**想认真运营获客建议用企业主体**。
4. 登录后进入「开发管理 → 开发设置」，复制 **AppID（小程序ID）**，形如 `wx1234567890abcdef`。

---

## 第 2 步：安装微信开发者工具并打开工程

1. 下载「微信开发者工具」稳定版：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html （选 **稳定版 Stable Build**，Windows 选 64 位）。
2. 安装后用**微信扫码登录**开发者工具。
3. 导入项目：
   - 「项目」→「导入项目」→ 目录选 `E:\code\e-protractor`。
   - **AppID** 一栏：选「填写 AppID」，粘贴你第 1 步复制的 `wx...`。
   - 后端服务选「不使用云服务」（本程序纯本地，无需云）。
   - 点「导入」。
4. 导入后工具会自动编译。左侧模拟器应出现深色「电子量角器」界面。

> 如果编译报错，优先检查：AppID 是否填对、目录是否选到包含 `app.json` 的根目录。

---

## 第 3 步：模拟器里先做基础验证（约 5 分钟）

在电脑上点「开始测量」——注意：**模拟器没有真实传感器，角度不会动**，这是正常的。模拟器只用于验证：
- 页面布局是否正常（HUD 数字、仪表盘、按钮）。
- 点 HUD 能否在 `° / rad` 间切换。
- 点「设基准」是否弹提示（未开始测量时）。
- 切到「历史」tab 是否正常。

真正的功能验证**必须在真机**，见第 4 步。

---

## 第 4 步：真机预览（最快看到手机上的效果）

1. 开发者工具顶部工具栏点 **「预览」**（二维码图标）。
2. 用**注册该小程序的同一个微信**扫二维码 → 手机自动打开小程序。
3. 在 iPhone 上点「开始测量」：
   - 首次会弹 **iOS 系统「微信想访问你的运动与健身」** 授权 → 必须点「允许」，否则传感器读不到角度。
   - 平放手机在桌面，慢慢转动 → HUD 数字应实时变化、仪表盘刻度环旋转、接近 0° 时变绿并震动+音效。
   - 点「＋ 记录」→ 切到「历史」tab 能看到刚记的一条。
4. 此模式叫「预览版」，**每次重新预览二维码会变**，仅你自己可用，适合快速验证。

> 💡 如果手机上完全没反应/角度不动：大概率是运动权限被拒。到 iPhone「设置 → 微信 → 运动与健身」打开，或删除小程序重进重新授权。

---

## 第 5 步：真机调试（排查问题用）

当预览表现不对时，用「真机调试」而不是反复预览：
1. 工具栏点 **「真机调试」**（类似bug图标）→ 扫码。
2. 手机运行的同时，电脑端会弹出 **vConsole + 远程调试器**：
   - **Console** 看 `console.log` 报错。
   - **Network / Storage** 看本地存储写入。
   - 可以直接在 Sources 里打断点、改代码热更新。
3. 真机调试适合验证：音效是否播放、震动是否触发、Canvas 绘制是否正确、权限弹窗走向。

> 音频 `tick.wav/snap.wav` 已设 `obeyMuteSwitch:false`，即使用户开了静音也会响；若真机仍无声，检查 iPhone 是否开了「铃声/静音」物理键，以及微信是否被系统禁止声音。

---

## 第 6 步：上传代码（准备提审）

功能在真机验证 OK 后：
1. 工具栏点 **「上传」**（上箭头图标）。
2. 填写：
   - **版本号**：第一版填 `1.0.0`。
   - **项目备注**：如「电子量角器首发版：相对角度测量、磁吸归零、1000条记录」。
3. 上传成功 → 代码进入 **MP 后台的「开发管理 → 开发版本」**。

> 上传前建议点工具栏「详情 → 本地代码」确认包体很小（本程序几 KB + 两个 WAV，远低于 2MB 主包上限）。

---

## 第 7 步：配置体验版（给同事/好友先测，可选）

1. 在 MP 后台「成员管理 → 体验成员」添加微信号。
2. 「开发管理 → 开发版本」里把某个版本「设为体验版」。
3. 体验成员扫体验码即可使用（无需审核），适合小范围内测收集反馈。

---

## 第 8 步：提交审核（最关键的过审环节）

1. 在 MP 后台「开发管理 → 开发版本」点 **「提交审核」**。
2. 填写审核信息：
   - **功能页面**：填 `pages/index/index`（测量主页路径）。
   - **类目**：选 **「工具」→「实用工具」**（或「效率」）。类目选错是常见被拒原因。
   - **标签**：电子量角器、角度测量、工具、iOS。
   - **小程序简介/名称**：清晰描述功能，避免与系统「测距仪/水平仪」混淆以免商标风险。
   - **测试账号**：选「无需」——本程序纯本地、不上传数据。
3. **必做：完善隐私合规**
   - 「设置 → 服务内容 → 用户隐私保护指引」中勾选本程序收集的字段（本程序**不收集个人信息**，可声明「不收集」/仅必要的传感器使用说明），并发布隐私政策。
   - 代码已调用 `wx.requirePrivacyAuthorize` 在首次进入弹授权（`__usePrivacyCheck__:true`），与后台声明一致。
4. 提交 → 进入审核队列（通常几小时到 1–2 个工作日）。

---

## 第 9 步：发布上线

1. 审核通过后，MP 后台该版本状态变「审核通过」。
2. 点 **「发布」** → 小程序正式上线，所有微信用户可搜索/扫码使用。
3. 之后迭代：改代码 → 上传 → 提审 → 发布，循环即可。

---

## 📱 电子量角器专属真机自测清单（提交前逐项过）

| 项目 | 预期 | 备注 |
| --- | --- | --- |
| 运动权限 | 首次点开始测量弹系统授权，允许后能读数 | iOS 设置→微信→运动与健身 |
| 相对角度 | 平放转动，HUD 实时变化，顺时针为正 | 立握转方向盘场景不理想（见下） |
| 设基准 | 点「设基准」后当前角度归 0 并震动+闭锁音 | 一键扣除静态偏差 |
| 磁吸归零 | 接近 0°±0.5° 变绿 + heavy 震动 + snap 音 | 关键手感 |
| 单位切换 | 点 HUD 在 °/rad 切换并持久化 | 存 `unit_preference` |
| 记录 | 「＋记录」写入，历史页可见 | FIFO 上限 1000 |
| CSV 导出 | 历史页「导出CSV」→ 复制剪贴板成功 | 可粘到 Excel |
| 倾斜提示 | 手机拿歪 >15° 显示「请贴紧测量面」 | 精度保护 |
| Auto-Hold | 静止 1s 自动锁定高亮 | 点提示可解除 |
| 分享 | 转发给好友/朋友圈带测量值文案 | 冷启动获客 |

---

## ⚠️ 上线前必须知道的两点限制

1. **最佳场景是「平放旋转」**：本版以 DeviceMotion `alpha`（绕 Z 轴方向）为基准。若想支持「立握像转方向盘」，需改用 `gamma`（绕 Y 轴），可下版加「测量模式」切换。
2. **采样频率上限**：`interval:'game'` 约 50Hz，达不到 120Hz（ProMotion）。手感已尽量调优，但这是平台 API 上限，非代码问题。

---

## 常见问题速查

- **预览/真机调试报错「appid 无效」**：你还在用 `touristappid`，换成真实 AppID 并重新导入。
- **真机角度不动**：iOS 运动权限被拒 → 设置里打开，或删小程序重授权。
- **审核被拒「类目不符」**：确认类目在「工具」下，且功能页截图与描述一致。
- **审核被拒「隐私」**：后台补齐隐私保护指引并发布，与代码授权弹窗对应。
- **音效不响**：确认 WAV 在 `assets/audio/` 且 `obeyMuteSwitch:false`；检查手机未对微信禁声。
